using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class T_Enemy : Human
{
    // Start is called before the first frame update
    void Start()
    {
        statController.Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
